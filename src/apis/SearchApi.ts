/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace: notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) or an OAuth 2.1 access token, and use HTTPS.  Official SDKs (MIT, generated from this spec on every release):  - TypeScript: https://github.com/spatio-labs/spatio-ts (`npm install @spatio-labs/spatio-ts`) - Python: https://github.com/spatio-labs/spatio-py (`pip install spatio-sdk`) - Go: https://github.com/spatio-labs/spatio-go (`go get github.com/spatio-labs/spatio-go`)  This specification is generated from the platform-service Go source on every push to `main`. The spec, not hand-written documentation, is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type FederatedSearch200Response,
    FederatedSearch200ResponseFromJSON,
    FederatedSearch200ResponseToJSON,
} from '../models/FederatedSearch200Response';
import {
    type FederatedSearchRequest,
    FederatedSearchRequestFromJSON,
    FederatedSearchRequestToJSON,
} from '../models/FederatedSearchRequest';

export interface FederatedSearchOperationRequest {
    federatedSearchRequest: FederatedSearchRequest;
}

/**
 * 
 */
export class SearchApi extends runtime.BaseAPI {

    /**
     * Creates request options for federatedSearch without sending the request
     */
    async federatedSearchRequestOpts(requestParameters: FederatedSearchOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['federatedSearchRequest'] == null) {
            throw new runtime.RequiredError(
                'federatedSearchRequest',
                'Required parameter "federatedSearchRequest" was null or undefined when calling federatedSearch().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/search`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FederatedSearchRequestToJSON(requestParameters['federatedSearchRequest']),
        };
    }

    /**
     * Fans out to every platform\'s per-platform search method in parallel, merges + dedupes results, and returns them in a relevance-then-recency ranking with per-platform cursors for pagination. 
     * Cross-platform federated search.
     */
    async federatedSearchRaw(requestParameters: FederatedSearchOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FederatedSearch200Response>> {
        const requestOptions = await this.federatedSearchRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FederatedSearch200ResponseFromJSON(jsonValue));
    }

    /**
     * Fans out to every platform\'s per-platform search method in parallel, merges + dedupes results, and returns them in a relevance-then-recency ranking with per-platform cursors for pagination. 
     * Cross-platform federated search.
     */
    async federatedSearch(requestParameters: FederatedSearchOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FederatedSearch200Response> {
        const response = await this.federatedSearchRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
