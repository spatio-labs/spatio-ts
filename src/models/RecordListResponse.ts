/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace: notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) or an OAuth 2.1 access token, and use HTTPS.  Official SDKs (MIT, generated from this spec on every release):  - TypeScript: https://github.com/spatio-labs/spatio-ts (`npm install @spatio/sdk-ts`) - Python: https://github.com/spatio-labs/spatio-py (`pip install spatio-sdk`) - Go: https://github.com/spatio-labs/spatio-go (`go get github.com/spatio-labs/spatio-go`)  This specification is generated from the platform-service Go source on every push to `main`. The spec, not hand-written documentation, is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ModelRecord } from './ModelRecord';
import {
    ModelRecordFromJSON,
    ModelRecordFromJSONTyped,
    ModelRecordToJSON,
    ModelRecordToJSONTyped,
} from './ModelRecord';

/**
 * 
 * @export
 * @interface RecordListResponse
 */
export interface RecordListResponse {
    /**
     * 
     * @type {Array<ModelRecord>}
     * @memberof RecordListResponse
     */
    records: Array<ModelRecord>;
    /**
     * 
     * @type {number}
     * @memberof RecordListResponse
     */
    total_results?: number;
    /**
     * 
     * @type {Date}
     * @memberof RecordListResponse
     */
    updated_at?: Date;
}

/**
 * Check if a given object implements the RecordListResponse interface.
 */
export function instanceOfRecordListResponse(value: object): value is RecordListResponse {
    if (!('records' in value) || value['records'] === undefined) return false;
    return true;
}

export function RecordListResponseFromJSON(json: any): RecordListResponse {
    return RecordListResponseFromJSONTyped(json, false);
}

export function RecordListResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): RecordListResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'records': ((json['records'] as Array<any>).map(ModelRecordFromJSON)),
        'total_results': json['total_results'] == null ? undefined : json['total_results'],
        'updated_at': json['updated_at'] == null ? undefined : (new Date(json['updated_at'])),
    };
}

export function RecordListResponseToJSON(json: any): RecordListResponse {
    return RecordListResponseToJSONTyped(json, false);
}

export function RecordListResponseToJSONTyped(value?: RecordListResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'records': ((value['records'] as Array<any>).map(ModelRecordToJSON)),
        'total_results': value['total_results'],
        'updated_at': value['updated_at'] == null ? value['updated_at'] : value['updated_at'].toISOString(),
    };
}

