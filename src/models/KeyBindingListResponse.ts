/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace — notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) and use HTTPS.  This specification is generated from the platform-service Go source on every push to `main`. The spec — not hand-written documentation — is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { KeyBinding } from './KeyBinding';
import {
    KeyBindingFromJSON,
    KeyBindingFromJSONTyped,
    KeyBindingToJSON,
    KeyBindingToJSONTyped,
} from './KeyBinding';

/**
 * 
 * @export
 * @interface KeyBindingListResponse
 */
export interface KeyBindingListResponse {
    /**
     * 
     * @type {Array<KeyBinding>}
     * @memberof KeyBindingListResponse
     */
    bindings: Array<KeyBinding>;
}

/**
 * Check if a given object implements the KeyBindingListResponse interface.
 */
export function instanceOfKeyBindingListResponse(value: object): value is KeyBindingListResponse {
    if (!('bindings' in value) || value['bindings'] === undefined) return false;
    return true;
}

export function KeyBindingListResponseFromJSON(json: any): KeyBindingListResponse {
    return KeyBindingListResponseFromJSONTyped(json, false);
}

export function KeyBindingListResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): KeyBindingListResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'bindings': ((json['bindings'] as Array<any>).map(KeyBindingFromJSON)),
    };
}

export function KeyBindingListResponseToJSON(json: any): KeyBindingListResponse {
    return KeyBindingListResponseToJSONTyped(json, false);
}

export function KeyBindingListResponseToJSONTyped(value?: KeyBindingListResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'bindings': ((value['bindings'] as Array<any>).map(KeyBindingToJSON)),
    };
}

