/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace: notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) or an OAuth 2.1 access token, and use HTTPS.  Official SDKs (MIT, generated from this spec on every release):  - TypeScript: https://github.com/spatio-labs/spatio-ts (`npm install @spatio/sdk-ts`) - Python: https://github.com/spatio-labs/spatio-py (`pip install spatio-sdk`) - Go: https://github.com/spatio-labs/spatio-go (`go get github.com/spatio-labs/spatio-go`)  This specification is generated from the platform-service Go source on every push to `main`. The spec, not hand-written documentation, is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { AccountStatus } from './AccountStatus';
import {
    AccountStatusFromJSON,
    AccountStatusFromJSONTyped,
    AccountStatusToJSON,
    AccountStatusToJSONTyped,
} from './AccountStatus';
import type { Note } from './Note';
import {
    NoteFromJSON,
    NoteFromJSONTyped,
    NoteToJSON,
    NoteToJSONTyped,
} from './Note';

/**
 * Standard fan-out response for `GET /v1/notes`. Items aggregate notes
 * across every connected account that contributed to the call; each
 * contributing account contributes one `accounts[]` entry — including
 * failed accounts, so the client can render which providers contributed
 * and which errored without the call itself failing.
 * 
 * @export
 * @interface NoteListEnvelope
 */
export interface NoteListEnvelope {
    /**
     * 
     * @type {Array<Note>}
     * @memberof NoteListEnvelope
     */
    items: Array<Note>;
    /**
     * 
     * @type {Array<AccountStatus>}
     * @memberof NoteListEnvelope
     */
    accounts: Array<AccountStatus>;
}

/**
 * Check if a given object implements the NoteListEnvelope interface.
 */
export function instanceOfNoteListEnvelope(value: object): value is NoteListEnvelope {
    if (!('items' in value) || value['items'] === undefined) return false;
    if (!('accounts' in value) || value['accounts'] === undefined) return false;
    return true;
}

export function NoteListEnvelopeFromJSON(json: any): NoteListEnvelope {
    return NoteListEnvelopeFromJSONTyped(json, false);
}

export function NoteListEnvelopeFromJSONTyped(json: any, ignoreDiscriminator: boolean): NoteListEnvelope {
    if (json == null) {
        return json;
    }
    return {
        
        'items': ((json['items'] as Array<any>).map(NoteFromJSON)),
        'accounts': ((json['accounts'] as Array<any>).map(AccountStatusFromJSON)),
    };
}

export function NoteListEnvelopeToJSON(json: any): NoteListEnvelope {
    return NoteListEnvelopeToJSONTyped(json, false);
}

export function NoteListEnvelopeToJSONTyped(value?: NoteListEnvelope | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'items': ((value['items'] as Array<any>).map(NoteToJSON)),
        'accounts': ((value['accounts'] as Array<any>).map(AccountStatusToJSON)),
    };
}

