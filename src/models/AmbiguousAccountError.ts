/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace: notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) or an OAuth 2.1 access token, and use HTTPS.  Official SDKs (MIT, generated from this spec on every release):  - TypeScript: https://github.com/spatio-labs/spatio-ts (`npm install @spatio-labs/spatio-ts`) - Python: https://github.com/spatio-labs/spatio-py (`pip install spatio-sdk`) - Go: https://github.com/spatio-labs/spatio-go (`go get github.com/spatio-labs/spatio-go`)  This specification is generated from the platform-service Go source on every push to `main`. The spec, not hand-written documentation, is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { AccountChoice } from './AccountChoice';
import {
    AccountChoiceFromJSON,
    AccountChoiceFromJSONTyped,
    AccountChoiceToJSON,
    AccountChoiceToJSONTyped,
} from './AccountChoice';

/**
 * Returned when the caller's request matches more than one connected
 * account and no `accountId` query param disambiguates which one to
 * target. The `accounts` array enumerates the candidates so the client
 * can prompt the user to pick.
 * 
 * @export
 * @interface AmbiguousAccountError
 */
export interface AmbiguousAccountError {
    /**
     * Human-readable error message.
     * @type {string}
     * @memberof AmbiguousAccountError
     */
    error: string;
    /**
     * Machine-readable error code. Stable across releases for the
     * canonical codes (`ambiguous_account`, `no_notes_provider`,
     * `note_not_found`). Absent for generic errors.
     * 
     * @type {string}
     * @memberof AmbiguousAccountError
     */
    code?: string;
    /**
     * 
     * @type {Array<AccountChoice>}
     * @memberof AmbiguousAccountError
     */
    accounts?: Array<AccountChoice>;
}

/**
 * Check if a given object implements the AmbiguousAccountError interface.
 */
export function instanceOfAmbiguousAccountError(value: object): value is AmbiguousAccountError {
    if (!('error' in value) || value['error'] === undefined) return false;
    return true;
}

export function AmbiguousAccountErrorFromJSON(json: any): AmbiguousAccountError {
    return AmbiguousAccountErrorFromJSONTyped(json, false);
}

export function AmbiguousAccountErrorFromJSONTyped(json: any, ignoreDiscriminator: boolean): AmbiguousAccountError {
    if (json == null) {
        return json;
    }
    return {
        
        'error': json['error'],
        'code': json['code'] == null ? undefined : json['code'],
        'accounts': json['accounts'] == null ? undefined : ((json['accounts'] as Array<any>).map(AccountChoiceFromJSON)),
    };
}

export function AmbiguousAccountErrorToJSON(json: any): AmbiguousAccountError {
    return AmbiguousAccountErrorToJSONTyped(json, false);
}

export function AmbiguousAccountErrorToJSONTyped(value?: AmbiguousAccountError | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'error': value['error'],
        'code': value['code'],
        'accounts': value['accounts'] == null ? undefined : ((value['accounts'] as Array<any>).map(AccountChoiceToJSON)),
    };
}

