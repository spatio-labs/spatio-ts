/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace — notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) and use HTTPS.  This specification is generated from the platform-service Go source on every push to `main`. The spec — not hand-written documentation — is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { AccountStatus } from './AccountStatus';
import {
    AccountStatusFromJSON,
    AccountStatusFromJSONTyped,
    AccountStatusToJSON,
    AccountStatusToJSONTyped,
} from './AccountStatus';
import type { Folder } from './Folder';
import {
    FolderFromJSON,
    FolderFromJSONTyped,
    FolderToJSON,
    FolderToJSONTyped,
} from './Folder';

/**
 * Fan-out list response for `GET /v1/files/folders`.
 * @export
 * @interface FolderListEnvelope
 */
export interface FolderListEnvelope {
    /**
     * 
     * @type {Array<Folder>}
     * @memberof FolderListEnvelope
     */
    items: Array<Folder> | null;
    /**
     * 
     * @type {Array<AccountStatus>}
     * @memberof FolderListEnvelope
     */
    accounts: Array<AccountStatus>;
}

/**
 * Check if a given object implements the FolderListEnvelope interface.
 */
export function instanceOfFolderListEnvelope(value: object): value is FolderListEnvelope {
    if (!('items' in value) || value['items'] === undefined) return false;
    if (!('accounts' in value) || value['accounts'] === undefined) return false;
    return true;
}

export function FolderListEnvelopeFromJSON(json: any): FolderListEnvelope {
    return FolderListEnvelopeFromJSONTyped(json, false);
}

export function FolderListEnvelopeFromJSONTyped(json: any, ignoreDiscriminator: boolean): FolderListEnvelope {
    if (json == null) {
        return json;
    }
    return {
        
        'items': (json['items'] == null ? null : (json['items'] as Array<any>).map(FolderFromJSON)),
        'accounts': ((json['accounts'] as Array<any>).map(AccountStatusFromJSON)),
    };
}

export function FolderListEnvelopeToJSON(json: any): FolderListEnvelope {
    return FolderListEnvelopeToJSONTyped(json, false);
}

export function FolderListEnvelopeToJSONTyped(value?: FolderListEnvelope | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'items': (value['items'] == null ? null : (value['items'] as Array<any>).map(FolderToJSON)),
        'accounts': ((value['accounts'] as Array<any>).map(AccountStatusToJSON)),
    };
}

