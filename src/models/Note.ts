/* tslint:disable */
/* eslint-disable */
/**
 * SpatioAPI
 * The REST API that owns every resource in your Spatio workspace — notes, sheets, slides, tasks, calendar events, mail, chat, files, and contacts. SpatioMCP wraps this API; Spatio Desktop reads from it. You can call it directly from your own code.  All requests must be authenticated with a Personal Access Token (`Authorization: Bearer pat_...`) and use HTTPS.  This specification is generated from the platform-service Go source on every push to `main`. The spec — not hand-written documentation — is the source of truth: server stubs and SDKs are generated from it, and any drift between the spec and the running service fails CI. 
 *
 * The version of the OpenAPI document: v1
 * Contact: hello@spatio.app
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * A markdown note. Notes belong to exactly one connected account
 * (`accountId` + `provider`). The native provider stores notes in the
 * Spatio database; external providers (Notion, Google Keep, etc.) store
 * them upstream and round-trip through Spatio.
 * 
 * @export
 * @interface Note
 */
export interface Note {
    /**
     * Stable provider id for the note.
     * @type {string}
     * @memberof Note
     */
    id: string;
    /**
     * Registered provider id (e.g. `native-notes`).
     * @type {string}
     * @memberof Note
     */
    provider?: string;
    /**
     * Connected-account row this note belongs to.
     * @type {string}
     * @memberof Note
     */
    accountId?: string;
    /**
     * User id of the note's owner. Surfaced so the renderer can show
     * "Shared with you" when `ownerUserId` differs from the viewer's
     * id. Empty for non-native providers.
     * 
     * @type {string}
     * @memberof Note
     */
    ownerUserId?: string;
    /**
     * 
     * @type {string}
     * @memberof Note
     */
    title: string;
    /**
     * Markdown body. The block tree at `/v1/notes/{id}/blocks` is the
     * canonical structured representation; `content` is a flattened
     * markdown view kept for clients that don't render blocks.
     * 
     * @type {string}
     * @memberof Note
     */
    content: string;
    /**
     * Emoji or short string used as the note's icon.
     * @type {string}
     * @memberof Note
     */
    icon?: string;
    /**
     * URL of the note's cover image.
     * @type {string}
     * @memberof Note
     */
    coverImage?: string;
    /**
     * Parent note id when notes are nested.
     * @type {string}
     * @memberof Note
     */
    parentId?: string | null;
    /**
     * Free-form provider-specific properties (tags, etc.).
     * @type {{ [key: string]: any; }}
     * @memberof Note
     */
    properties?: { [key: string]: any; };
    /**
     * 
     * @type {boolean}
     * @memberof Note
     */
    archived: boolean;
    /**
     * 
     * @type {Date}
     * @memberof Note
     */
    createdAt: Date;
    /**
     * 
     * @type {Date}
     * @memberof Note
     */
    updatedAt: Date;
    /**
     * User id of the most recent editor.
     * @type {string}
     * @memberof Note
     */
    lastEditedBy?: string;
}

/**
 * Check if a given object implements the Note interface.
 */
export function instanceOfNote(value: object): value is Note {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('title' in value) || value['title'] === undefined) return false;
    if (!('content' in value) || value['content'] === undefined) return false;
    if (!('archived' in value) || value['archived'] === undefined) return false;
    if (!('createdAt' in value) || value['createdAt'] === undefined) return false;
    if (!('updatedAt' in value) || value['updatedAt'] === undefined) return false;
    return true;
}

export function NoteFromJSON(json: any): Note {
    return NoteFromJSONTyped(json, false);
}

export function NoteFromJSONTyped(json: any, ignoreDiscriminator: boolean): Note {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'provider': json['provider'] == null ? undefined : json['provider'],
        'accountId': json['accountId'] == null ? undefined : json['accountId'],
        'ownerUserId': json['ownerUserId'] == null ? undefined : json['ownerUserId'],
        'title': json['title'],
        'content': json['content'],
        'icon': json['icon'] == null ? undefined : json['icon'],
        'coverImage': json['coverImage'] == null ? undefined : json['coverImage'],
        'parentId': json['parentId'] == null ? undefined : json['parentId'],
        'properties': json['properties'] == null ? undefined : json['properties'],
        'archived': json['archived'],
        'createdAt': (new Date(json['createdAt'])),
        'updatedAt': (new Date(json['updatedAt'])),
        'lastEditedBy': json['lastEditedBy'] == null ? undefined : json['lastEditedBy'],
    };
}

export function NoteToJSON(json: any): Note {
    return NoteToJSONTyped(json, false);
}

export function NoteToJSONTyped(value?: Note | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'provider': value['provider'],
        'accountId': value['accountId'],
        'ownerUserId': value['ownerUserId'],
        'title': value['title'],
        'content': value['content'],
        'icon': value['icon'],
        'coverImage': value['coverImage'],
        'parentId': value['parentId'],
        'properties': value['properties'],
        'archived': value['archived'],
        'createdAt': value['createdAt'].toISOString(),
        'updatedAt': value['updatedAt'].toISOString(),
        'lastEditedBy': value['lastEditedBy'],
    };
}

